export interface UserType {
    id: number,
    imie: string,
    isAdmin: boolean,
    isLekarz: boolean,
    isPacjent: boolean,
    login: string,
    miasto: string,
    nazwisko: string,
    password: string,
    pesel: string,
}
